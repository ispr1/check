import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Kovanent Identity | Enterprise Verification Platform",
  description: "AI-powered identity verification and trust scoring for enterprise hiring. Verify candidates with confidence.",
  keywords: ["identity verification", "background check", "trust score", "HR verification", "enterprise"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
